name: Hello World
on: 
 push:
  branches:
   - main
   - develop
   - 'release/**'
   
 
jobs:
 build:
   runs-on: ubuntu-latest
   steps:
    - name: Build step
      run: echo "you are building it"

      
